services:
  vue-app:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    environment:
      - NODE_ENV=development
    volumes:
      - ./vueApp:/app
    command: sh -c "cd vue-sample && npm run serve"
    ports:
      - "8090:8080"
